coverage:
  status:
    project:
      default:
        target: 50%
        threshold: 1%
    patch:
      default:
        target: 50%
        threshold: 0%

comment:
  layout: 'header, reach, diff, flags, files, tree, footer, feedback'
  hide_project_coverage: false
  behavior: new
  require_changes: false
  require_base: false
  require_head: true
